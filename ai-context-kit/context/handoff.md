---
file: handoff
seq: 1
updated_utc: 1970-01-01T00:00Z
updated_by: unset
status: idle
next_action: "Read START-HERE.md, then ask the user for a scan level."
files: []
---

# Handoff — the one task in flight

**Update this while you work, not when you finish.** Fill in `files:` above with the exact paths
this task needs, so the next session loads those instead of re-deriving them by scanning.

## Active task

*Nothing in flight.*

<!-- When working, replace the line above with:

**Task.** <what is being done, with enough context to resume cold>

**Progress.** <what is already done within this task>

**Next action.** <the exact next step — specific enough to act on without re-reading anything>

**Waiting on.** <a pending decision, a build, the user — or nothing>
-->

---

## Traps worth remembering

Things that would otherwise be rediscovered painfully, by every AI, every time.

- <e.g. "the fast build skips asset staging and reports wrong output — always run the full build">
